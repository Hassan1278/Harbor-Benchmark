# Secrets
.env

# Generated benchmark tasks — regenerate with `python -m adapters.<name>.adapter`.
# Kept out of git because: (a) 220 dirs bloat the repo, (b) gold solutions are
# large binaries, (c) long Unicode filenames break Git on Windows.
datasets/

# Benchmark results (large, regenerable)
jobs/

# Downloaded frameworks (install via pip/harbor download)
harbor/
terminal-bench/

# Python
__pycache__/
*.pyc

# OS files
Thumbs.db
.DS_Store

# Claude Code local settings
.claude/

# Ollama modelfiles
Modelfile
