# Copy to .env and fill in. The .env file is gitignored.
#
# Judge priority (first with a key wins): Gemini -> Anthropic -> Ollama (free fallback)
# Paper-accurate judge is Gemini 3.1 Pro Preview.

GEMINI_API_KEY=""                     # https://aistudio.google.com/apikey
ANTHROPIC_API_KEY=""                  # https://console.anthropic.com/settings/keys
GROQ_API_KEY=""                       # https://console.groq.com/keys

# Judge mode:
#   rubric    (default) — score model output against JSON rubric → float 0-1
#   pairwise  — compare model output vs human gold (paper methodology) → {0, 0.5, 1}
# JUDGE_MODE=rubric

# Optional judge overrides
# JUDGE_PROVIDER=gemini|anthropic|ollama
# JUDGE_MODEL=gemini-3.1-pro-preview   # Gemini default (paper)
# JUDGE_MODEL=claude-sonnet-4-5        # Anthropic fallback
# JUDGE_MODEL=claude-haiku-4-5-20251001 # Cheap/fast Anthropic judge
# JUDGE_MODEL=gpt-oss:120b-cloud       # Ollama Cloud neutral judge

# --- Example: Claude Pro with Haiku everywhere ---
# JUDGE_PROVIDER=anthropic
# JUDGE_MODEL=claude-haiku-4-5-20251001
# (OAuth token is auto-extracted from ~/.claude/.credentials.json by run.sh)
# Then run: bash run.sh --claude --model anthropic/claude-haiku-4-5-20251001 -p datasets/gdpval -n 1
